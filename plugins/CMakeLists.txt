# Header files need not be enumerated
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

IF(ENABLE_HDF5_PLUGIN)
    LIST(APPEND plugin_srcs macsio_hdf5.c)
ENDIF(ENABLE_HDF5_PLUGIN)

IF(ENABLE_PDB_PLUGIN)
    LIST(APPEND plugin_srcs macsio_pbd.c)
ENDIF(ENABLE_PDB_PLUGIN)

IF(ENABLE_EXODUS_PLUGIN)
    LIST(APPEND plugin_srcs macsio_exodus.c)
ENDIF(ENABLE_EXODUS_PLUGIN)

IF(ENABLE_SILO_PLUGIN)
    LIST(APPEND plugin_srcs macsio_silo.c)
ENDIF(ENABLE_SILO_PLUGIN)

SET(PLUGIN_SRCS ${pluing_srcs} PARENT_SCOPE)
SET_SOURCE_FILES_PROPERTIES(${PLUGIN_SRCS} PROPERTIES LANGUAGE CXX)